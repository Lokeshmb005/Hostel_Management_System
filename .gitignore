# ─── Private / Secret Files ───────────────────────
# config.js is now committed intentionally (Firebase keys are safe to expose)
# The real security is enforced by Firestore Security Rules

# ─── OS / Editor junk ───────────────────────────
.DS_Store
Thumbs.db
*.log
.vscode/
